"""ML layer — scaffolded, deliberately empty in Phase 1.

No machine learning is built here (brief section 1): this package exists
so Phase 3 has a home, once `forexml.signals.SignalStore` has accumulated
enough labeled taken/rejected signals to train on.
"""
